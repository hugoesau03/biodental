# Secretos — NUNCA subir a GitHub
.env

# Base de datos local
*.db
*.sqlite
*.sqlite3

# Python
__pycache__/
*.py[cod]
*.pyo
.venv/
venv/
env/
*.egg-info/
dist/
build/

# Knowledge (archivos privados del negocio)
knowledge/*
!knowledge/.gitkeep

# Nota: agent/, config/, tests/, requirements.txt, Dockerfile y
# docker-compose.yml SÍ se versionan — son el código real del agente de
# Biodental, no un output descartable de la entrevista de onboarding
# genérica de AgentKit (que sí se ignoraba en la plantilla original).

# Session state
config/session.yaml

# OS
.DS_Store
Thumbs.db

# IDE
.vscode/
.idea/
*.swp
*.swo
*~

# Docker
.dockerignore

# Node (si se usa para Claude Code)
node_modules/
